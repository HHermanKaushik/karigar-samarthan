import 'package:flutter/material.dart';

/// Warm artisan-focused palette for Karigar Samarthan.
class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF7F4EE);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSoft = Color(0xFFFCFAF6);

  // Brand Colors
  static const Color primary = Color(0xFF243B6B); // deep indigo
  static const Color primaryDark = Color(0xFF1B2D52);

  static const Color secondary = Color(0xFFD98C2B); // saffron
  static const Color accent = Color(0xFF4F7C5A); // muted artisan green

  // Text
  static const Color text = Color(0xFF1E1E1E);
  static const Color textMuted = Color(0xFF6F6A63);

  // Borders
  static const Color border = Color(0xFFE7E0D4);
  static const Color borderDark = Color(0xFFD8CFC1);

  // Status
  static const Color success = Color(0xFF4F7C5A);
  static const Color warning = Color(0xFFD98C2B);
  static const Color danger = Color(0xFFB85450);

  // Shadows
  static const Color shadow = Color(0x14000000);

  // Pills / tags
  static const Color chipBackground = Color(0xFFF2EEE7);

  // AI Assistant
  static const Color aiBubble = Color(0xFFEAEFF8);
}
