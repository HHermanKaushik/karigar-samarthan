import 'package:flutter/material.dart';

/// Earthy, calm palette inspired by traditional Indian textiles.
class AppColors {
  static const Color background = Color(0xFFFBF7F0); // warm cream
  static const Color surface = Color(0xFFFFFFFF);
  static const Color primary = Color.fromRGBO(104, 63, 188, 1); // terracotta
  static const Color primaryDark = Color.fromRGBO(84, 20, 107, 1)
  static const Color secondary = Color(0xFFD4A24C); // turmeric gold
  static const Color accent = Color(0xFF2E5D4F); // deep henna green
  static const Color text = Color(0xFF1F1A14);
  static const Color textMuted = Color(0xFF6B5E50);
  static const Color border = Color(0xFFE6DDCB);
  static const Color danger = Color(0xFFB23A2A);
}
